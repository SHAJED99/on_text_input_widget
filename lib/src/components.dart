part of '../on_text_input_widget.dart';

const double _defaultPadding = 24;
const double _defaultHorizontalPadding = _defaultPadding / 3;
const double _defaultVerticalPadding = _defaultPadding / 4;
const double _defaultHeight = 48;
const EdgeInsets _defaultContentPadding = EdgeInsets.symmetric(
  horizontal: _defaultHorizontalPadding,
  vertical: _defaultVerticalPadding,
);

final BorderRadius _defaultBorderRadius =
    BorderRadius.circular(_defaultPadding / 3);
