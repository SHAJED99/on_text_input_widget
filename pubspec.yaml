name: on_text_input_widget
description: "A text field that makes so many things like online, and offline searching easier."
version: 0.0.3
homepage: https://github.com/SHAJED99/on_text_input_widget
license: BSD 3-Clause

screenshots:
  - description: 'Example of on_text_input_widget'
    path: screenshots/1.gif
  

topics:
  - text-input
  - text

environment:
  sdk: '>=2.12.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
